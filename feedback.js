let checked = document.getElementById('check');
let submit = document.getElementById('submit');
let feedback = document.getElementById('feedback');
const handleClick=() =>{
    checkbox.checked ? submit.innertext = "You have Subscribe the page & not selected any Experience" : submit.innertext = "You have not Subscribe the page & not selected any Experience ";
};

